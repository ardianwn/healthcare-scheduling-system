# ===========================================
# Healthcare Scheduling System Configuration
# ===========================================
# 
# ⚠️ SECURITY WARNING:
# This is a template file (.env.example) that is committed to GitHub.
# - Copy this file to .env: cp .env.example .env
# - Replace ALL placeholder values with your actual credentials
# - NEVER commit .env file to version control!
#
# ===========================================

# PostgreSQL Configuration (Docker Compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_AUTH_DB=auth_db
POSTGRES_SCHEDULE_DB=schedule_db

# Auth Service Configuration
# DATABASE_URL is auto-generated by docker-compose.yml
AUTH_DATABASE_URL=postgresql://postgres:your-secure-password-here@localhost:5432/auth_db
AUTH_JWT_SECRET=your-generated-jwt-secret-here-use-openssl-rand-hex-32
AUTH_JWT_EXPIRATION=24h
AUTH_PORT=3001

# Schedule Service Configuration
# DATABASE_URL is auto-generated by docker-compose.yml
SCHEDULE_DATABASE_URL=postgresql://postgres:your-secure-password-here@localhost:5433/schedule_db
SCHEDULE_PORT=3002
AUTH_SERVICE_URL=http://localhost:3001/graphql

# Redis Configuration (Queue & Cache)
REDIS_HOST=redis
REDIS_PORT=6379

# Email Configuration (Optional - system works without email)
# For Gmail, get App Password from: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password-16-chars
SMTP_FROM=your-email@gmail.com

# ===========================================
# Setup Instructions:
# ===========================================
# 1. Copy this file: cp .env.example .env
# 2. Generate JWT secret: openssl rand -hex 32
# 3. Set secure PostgreSQL password (for production)
# 4. Add Gmail credentials if you want email notifications (optional)
# 
# For local development, you can use these test values in .env:
#   POSTGRES_PASSWORD=postgres
#   AUTH_JWT_SECRET=27725aa421b4de66ad257142cfc7fa6e314ad8b3aaa872c3fc61ade1c15df27e
#   (Leave email empty - system works without it)
# ===========================================
